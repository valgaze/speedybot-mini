{
  "name": "worker",
  "version": "1.0.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^3.17.0",
    "typescript": "^4.8.4",
    "wrangler": "2.0.15"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler publish",
    "deploy:mini": "wrangler publish --minify"
  },
  "dependencies": {
    "speedybot-mini": "^2.0.1"
  }
}
