{
  "name": "mini-lambda",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "npm start",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove",
    "console": "sst console",
    "typecheck": "tsc --noEmit",
    "test": "sst load-config -- vitest run"
  },
  "devDependencies": {
    "@serverless-stack/cli": "^1.15.16",
    "@serverless-stack/resources": "^1.15.16",
    "@tsconfig/node16": "^1.0.3",
    "aws-cdk-lib": "2.39.1",
    "typescript": "^4.8.4",
    "vitest": "^0.24.3"
  },
  "workspaces": [
    "services"
  ],
  "dependencies": {
    "cross-fetch": "^3.1.5",
    "speedybot-mini": "2.0.0-beta.16"
  }
}
